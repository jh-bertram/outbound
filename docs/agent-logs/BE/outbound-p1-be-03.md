## [STAGE 1] RECEIVED
- **From:** ORC (orchestrator)
- **At:** 2026-07-11T19:39:38Z
- **Task ID:** outbound-p1-be-03
- **Message received:**
  > You are BE#4 executing task packet `outbound-p1-be-03` (Wave 2) of sprint outbound-p1-mvp. Working directory: /home/jhber/projects/outbound. Task: Read `.claude/tasks/outputs/outbound-p1-mvp-amend-PM-1783792028.md` and execute the `<task_packet>` with `<task_id>outbound-p1-be-03</task_id>` EXACTLY as written — single source of truth. Scope summary: `scripts/fetch-nps-content.ts` — fetch NPS Data API content (descriptions, photos, campgrounds with url + reservationUrl link-outs) for all 63 destinations into the content JSON the packet names. CRITICAL packet-ordered sequence: save a LIVE romo (Rocky Mountain) API response snapshot BEFORE writing the Zod schema, then derive the schema from the actual snapshot — never from assumed/trained API shape. Environment: NPS_API_KEY present in .env (name only verified); be-02 landed (commit 9b07724) with parks.json (63 destinations, parkCode field, seki/seki-kica share parkCode "seki"), loader at src/data/parks.ts; package.json fetch-data chain pre-wired; runs IN PARALLEL with fe-02a and be-04 (foreign files not mine); be polite API citizen (sequential/small-batch). Receipt requirements: (1) live romo snapshot saved BEFORE schema authorship w/ timestamp, schema derived+validated against it; (2) all 63 destinations covered (62 distinct parkCodes, seki shared strategy stated); (3) campground entries carry url + reservationUrl; (4) NO key material in outputs w/ grep evidence; (5) creates only script + data outputs, no package.json edit; (6) build+typecheck+lint green. Constraints: NO git ops, never commit/copy .env, don't touch other agents' files. …[truncated]

## [STAGE 2] PLAN
- **At:** 2026-07-11T19:45:00Z
- **Approach:**
  1. Confirm environment: package.json fetch-data pre-wired (verified), src/data/parks.ts loader exists (be-02 landed), 62 distinct parkCodes confirmed via node, seki shares parkCode "seki" with distinct routingCoords (verified), 12 not-drivable parkCodes confirmed, .env has NPS_API_KEY (40-char, presence-only check, never printed).
  2. Fetch a LIVE romo snapshot FIRST via curl (key passed only as shell env var reference `$NPS_API_KEY`, never echoed) hitting `/parks?parkCode=romo&fields=images,activities,entranceFees` and `/campgrounds?parkCode=romo`, saved to `scripts/nps-snapshot/romo-parks.json` + `scripts/nps-snapshot/romo-campgrounds.json` with a recorded fetch timestamp — BEFORE any Zod schema is written.
  3. Test whether NPS API accepts comma-separated `parkCode` lists for batching (politeness / call-count reduction) on both `/parks` and `/campgrounds` endpoints using the live snapshot step.
  4. Inspect the actual snapshot JSON shape (images[], campground sub-fields, entranceFees, activities) and hand-derive the Zod schema strictly from what was observed — write `src/data/nps-content.ts` schema + loader.
  5. Write `scripts/fetch-nps-content.ts`: loads dotenv, fails fast if NPS_API_KEY missing, reads distinct parkCodes from `src/data/parks.ts`, fetches `/parks` + `/campgrounds` (batched if supported, else small sequential batches with a short delay), parses `latLong` strings to numbers, extracts campground `url`/`reservationUrl`, and writes `src/data/nps-content.json` keyed by parkCode.
  6. Run the script via `npm run fetch-data` (or a scoped `tsx scripts/fetch-nps-content.ts` invocation) to generate the real committed JSON for all 62 parkCodes; verify with Zod at load (`src/data/nps-content.ts`) and via `npm run typecheck`.
  7. Grep script + JSON outputs for the literal key value (never print it) to confirm no leakage; grep for the key length pattern as an extra proxy check.
  8. Run `npm run build`, `npm run typecheck`, `npm run lint` and capture snippets.
  9. Do NOT touch package.json, do NOT commit .env, do NOT touch other agents' in-flight files (App.tsx, build-drive-matrix.ts, etc).
- **Files to create/modify:**
  - `scripts/nps-snapshot/romo-parks.json` (raw live snapshot, audit artifact) → schema derivation evidence
  - `scripts/nps-snapshot/romo-campgrounds.json` (raw live snapshot, audit artifact) → schema derivation evidence
  - `src/data/nps-content.ts` (Zod schema + typed loader) → typed contract for FE consumption
  - `src/data/nps-content.json` (committed static content, all 62 parkCodes) → final data artifact
  - `scripts/fetch-nps-content.ts` (build-time fetch script, re-runnable via `npm run fetch-data`) → the fetch script itself
- **Dependencies / assumptions:**
  - Node 24 native `fetch` available (confirmed via node -e); `dotenv` present in node_modules (confirmed).
  - `tsx` present at `node_modules/.bin/tsx` (confirmed, v4.23.0).
  - NPS API base `https://developer.nps.gov/api/v1`, auth via `X-Api-Key` header (RA findings 1,2).
  - Assume comma-separated `parkCode` batching MAY be supported; verify empirically rather than assume — will fall back to per-code sequential fetch with small delay if not.

### Checkpoint — 19:41:10
- Fetched LIVE romo snapshot BEFORE any schema code: `scripts/nps-snapshot/romo-parks.json` (HTTP 200, 10498 bytes) + `scripts/nps-snapshot/romo-campgrounds.json` (HTTP 200, 41032 bytes), fetch timestamp `2026-07-11T19:41:10Z` recorded in `scripts/nps-snapshot/.fetch-timestamp`. Key passed via shell env var reference only (`$NPS_API_KEY`), never echoed/printed. Inspected shapes: `/parks` returns `{total,limit,start,data[]}`; park object has `images[]` ({credit,title,altText,caption,url}), `activities[]` ({id,name}), `entranceFees[]` ({cost,description,title}), plus scalar `latitude`/`longitude` (string) fields (cleaner than parsing the `latLong` string). `/campgrounds` returns `{total,limit,start,data[]}`; campground object has `url`, `reservationUrl`, `latitude`/`longitude` (string), plus much more (amenities, accessibility, etc — not needed for link-out scope). Also confirmed via a 3-park batch test (romo,acad,zion) that the API supports comma-separated `parkCode` lists on BOTH `/parks` and `/campgrounds` — enables a small-batch fetch instead of 124 sequential calls (politeness). Next: derive Zod schema from these observed shapes.
- Wrote `src/data/nps-content.ts` (Zod schema derived strictly from the live snapshot + typed loader, ~62 lines). Next: write scripts/fetch-nps-content.ts and generate src/data/nps-content.json.

### Checkpoint — 19:52:00
- Wrote `scripts/fetch-nps-content.ts` (~165 lines: fail-fast key load via native `process.loadEnvFile` (no undeclared dotenv dependency), batched /parks + /campgrounds fetch via confirmed comma-separated parkCode support, coordinate parsing, Zod-validated write). Wrote placeholder `src/data/nps-content.json` (`{}`) so the loader could bootstrap, then ran `npx tsx scripts/fetch-nps-content.ts` for real — succeeded, wrote 62 parkCode entries (723766 bytes). Verified: all 62 distinct parkCodes present (0 missing, 0 extra); seki (shared record, 15 campgrounds combining Sequoia+Kings Canyon), jeff (Gateway Arch, 0 campgrounds — correct, urban park), neri (New River Gorge, 9 campgrounds, some with empty url/reservationUrl — schema correctly tolerates this) all spot-checked. Fresh re-load via a separate tsx process confirms Zod validation passes against the real generated JSON (not just the placeholder). Grep evidence: `$NPS_API_KEY` value (40 chars) appears ZERO times in fetch-nps-content.ts, nps-content.ts, nps-content.json, or either snapshot file; recursive grep across src/ + scripts/ = 0 matches; only the literal string `NPS_API_KEY` (env var NAME, not value) appears, in the fail-fast check. Next: build/typecheck/lint gate.

### Checkpoint — 20:00:00
- `npm run typecheck` initially FAILED with TS1543 (JSON import under nodenext requires `with { type: "json" }`) on BOTH `src/data/nps-content.ts` (mine) and `src/data/parks.ts` (be-02's, pulled in transitively via my script's import — NOT mine to edit). Root cause: `scripts/**/*.ts` builds under tsconfig.node.json (`module: nodenext`), which applies program-wide to any file transitively imported, including be-02's `parks.ts` (authored under tsconfig.app.json's `bundler` resolution). Fix (no edits to parks.ts): (1) split the Zod schema out of `src/data/nps-content.ts` into a new `src/data/nps-content-schema.ts` with NO JSON import, so it can be shared cleanly by both the `bundler`-mode app loader and the `nodenext`-mode fetch script; (2) `nps-content.ts` now imports the JSON with the explicit `with { type: "json" }` attribute; (3) `scripts/fetch-nps-content.ts` now imports the parkCode list directly from `../src/data/parks.json` (with the same import attribute) instead of importing be-02's `parks.ts` loader, sidestepping the cross-module-mode conflict entirely without touching be-02's file. Re-ran `npm run typecheck` — PASSED. Re-ran the fetch script end-to-end post-refactor — still writes 62 valid entries; fresh Zod re-load still passes. DRY cleanup: removed a duplicated `chunk(...)` call (parkBatches/cgBatches were identical) — consolidated to one `batches` const reused by both loops. Final gate: `npm run typecheck` PASS, `npm run lint` PASS (zero output), `npm run build` PASS (vite build succeeds, dist/ emitted). `npm run test` has ONE pre-existing foreign-file failure: `e2e/map-mount.spec.ts` (fe-02a's Playwright spec, running in parallel) is picked up by vitest because `vitest.config.ts` (be-01's file) doesn't exclude `e2e/**` from the vitest glob — re-ran once, same result, confirmed NOT caused by my files (12/12 real vitest unit tests still pass); noted for ORC, out of my scope to fix. Final grep pass: zero occurrences of the NPS_API_KEY value anywhere in the repo outside `.env` (gitignored); `package.json` untouched (`git diff --stat package.json` empty, not in `git status`). Campground coverage: 298 total campground entries across 62 parkCodes, 256 with a populated `url`, 188 with a populated `reservationUrl` (some campgrounds legitimately lack one or both per the live API data — not a bug).

## [STAGE 3] COMPLETE
- **At:** 2026-07-11T20:05:00Z
- **Deliverables:**

| File | Size/Lines | Notes |
|---|---|---|
| `scripts/nps-snapshot/romo-parks.json` | 10,498 bytes | Live `/parks?parkCode=romo&fields=images,activities,entranceFees` snapshot, fetched 2026-07-11T19:41:10Z, BEFORE schema authorship |
| `scripts/nps-snapshot/romo-campgrounds.json` | 41,032 bytes | Live `/campgrounds?parkCode=romo` snapshot, same timestamp |
| `scripts/nps-snapshot/.fetch-timestamp` | 1 line | Recorded fetch timestamp for audit |
| `src/data/nps-content-schema.ts` | 63 lines | Zod schema (NpsParkContentSchema/NpsContentSchema) derived strictly from the live snapshot; isolated from JSON import so it's importable under both `bundler` and `nodenext` module resolution |
| `src/data/nps-content.ts` | 9 lines | Thin loader: imports the JSON (with `type: "json"` attribute) + schema, validates at load, re-exports schema/types |
| `src/data/nps-content.json` | 723,766 bytes | Committed static content, 62/62 parkCode entries, generated by a real live fetch |
| `scripts/fetch-nps-content.ts` | ~204 lines | Build-time fetch script; fail-fast key load; batched (chunk=15) `/parks` + `/campgrounds` fetch using confirmed comma-separated parkCode batching; Zod-validated write |

- **Lint / tests:** `npm run typecheck` PASS · `npm run lint` PASS (0 errors/warnings) · `npm run build` PASS · `npm run test`: 12/12 real vitest unit tests PASS, 1 pre-existing FOREIGN-FILE failure (`e2e/map-mount.spec.ts`, fe-02a's spec picked up by vitest due to be-01's `vitest.config.ts` not excluding `e2e/**` — not my file, reported to ORC).
- **Open items:** ORC/be-01 should add an `exclude: ['e2e/**']` (or similar) to `vitest.config.ts` so Playwright specs aren't picked up by the vitest runner — this is a cross-cutting config issue affecting the whole Wave 2 parallel set, not specific to my task.
