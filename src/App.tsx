// Scaffold placeholder — outbound-p1-be-01 owns config/scaffold only.
// outbound-p1-fe-02a is the sole creator of the real composition root
// (full-viewport map + slot components) and replaces this file's contents.
function App() {
  return (
    <main>
      <h1>Outbound</h1>
      <p>Scaffold ready.</p>
    </main>
  )
}

export default App
